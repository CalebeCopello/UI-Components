{
  "name": "ui-components",
  "version": "1.0.0",
  "description": "UI Components to use in my projects",
  "main": "server.js",
  "scripts": {
    "server": "nodemon backend/server.js",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "Calebe S. Copello",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.18.2",
    "nodemon": "^3.0.2"
  }
}
